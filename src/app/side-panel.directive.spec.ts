import { SidePanelDirective } from './side-panel.directive';

describe('SidePanelDirective', () => {
  it('should create an instance', () => {
    const directive = new SidePanelDirective();
    expect(directive).toBeTruthy();
  });
});
